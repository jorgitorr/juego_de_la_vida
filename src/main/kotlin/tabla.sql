CREATE DATABASE juegodelavida

CREATE OR REPLACE TABLE tabla(
    generacion VARCHAR(50),
    x VARCHAR(50),
    y VARCHAR(50)
);

CREATE OR REPLACE TABLE tamaniotablas(
    fila INT,
    columna INT
)